{
  "name": "kin-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "KIN Companion — Desktop menu bar app (Tauri v2)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
