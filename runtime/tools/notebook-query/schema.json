{
  "name": "notebook_query",
  "description": "Query Cipher's design, policy, architecture, and implementation knowledge base.",
  "parameters": {
    "type": "object",
    "properties": {
      "question": {
        "type": "string",
        "description": "The specific question to ask the knowledge base."
      },
      "top_k": {
        "type": "integer",
        "description": "Maximum number of relevant results to retrieve.",
        "default": 5,
        "minimum": 1,
        "maximum": 10
      },
      "scope": {
        "type": "string",
        "description": "Optional scope such as design, policy, onboarding, local-model, hosting, or general.",
        "enum": ["design", "policy", "onboarding", "creative-web", "local-model", "hosting", "general"]
      }
    },
    "required": ["question"]
  }
}
